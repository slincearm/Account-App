# 多國語言支援 (i18n)

本專案已整合 **i18next** 和 **react-i18next** 提供多國語言支援。

## 支援的語言

- 🇺🇸 **English** (en)
- 🇹🇼 **繁體中文** (zh-TW)
- 🇨🇳 **简体中文** (zh-CN)

## 使用方式

### 1. 在元件中使用翻譯

```jsx
import { useTranslation } from "react-i18next";

export default function MyComponent() {
  const { t } = useTranslation();

  return (
    <div>
      <h1>{t('dashboard.title')}</h1>
      <p>{t('common.welcome')}</p>
    </div>
  );
}
```

### 2. 切換語言

使用頁面右上角的語言切換器（地球圖示）即可切換語言。

### 3. 新增翻譯內容

翻譯檔案位於 `src/i18n/locales/` 目錄：

- `en.json` - 英文
- `zh-TW.json` - 繁體中文
- `zh-CN.json` - 简体中文

#### 新增翻譯鍵值

在各個語言檔案中加入對應的翻譯：

```json
// en.json
{
  "myFeature": {
    "title": "My Feature",
    "description": "This is my feature"
  }
}
```

```json
// zh-TW.json
{
  "myFeature": {
    "title": "我的功能",
    "description": "這是我的功能"
  }
}
```

### 4. 翻譯檔案結構

```
src/
└── i18n/
    ├── config.js          # i18n 初始化設定
    ├── locales/
    │   ├── en.json        # 英文翻譯
    │   ├── zh-TW.json     # 繁體中文翻譯
    │   └── zh-CN.json     # 简体中文翻譯
    └── ...
```

### 5. 語言偵測

- 系統會自動偵測瀏覽器語言
- 使用者選擇的語言會儲存在 `localStorage`
- 下次訪問時會自動載入上次選擇的語言

### 6. 新增新語言

1. 在 `src/i18n/locales/` 目錄新增語言檔案（例如：`ja.json`）
2. 在 `src/i18n/config.js` 中引入並註冊：

```javascript
import ja from "./locales/ja.json";

const resources = {
  en: { translation: en },
  "zh-TW": { translation: zhTW },
  "zh-CN": { translation: zhCN },
  ja: { translation: ja }  // 新增日文
};
```

3. 在 `src/contexts/LanguageContext.jsx` 中新增語言選項：

```javascript
const languages = [
  { code: "en", name: "English", flag: "🇺🇸" },
  { code: "zh-TW", name: "繁體中文", flag: "🇹🇼" },
  { code: "zh-CN", name: "简体中文", flag: "🇨🇳" },
  { code: "ja", name: "日本語", flag: "🇯🇵" }  // 新增日文
];
```

## 技術細節

- **i18next**: 國際化框架核心
- **react-i18next**: React 整合層
- **localStorage**: 語言偏好設定持久化
- **Context API**: 語言狀態管理

## 測試

啟動開發伺服器後，點擊右上角的語言切換器測試不同語言：

```bash
npm run dev
```

## 注意事項

- 所有使用者可見的文字都應該使用 `t()` 函數包裹
- 確保所有語言檔案中的鍵值都保持一致
- 新增功能時記得同步更新所有語言檔案
